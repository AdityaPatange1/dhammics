services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - dhammics_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: dhammics

volumes:
  dhammics_mongo_data:

# Frontend — injected into assets/js/env-config.js by scripts/inject-frontend-env.js
# Run `npm run dev` or `npm run build` after changing this (or run the inject script alone).
DHAMMICS_API_BASE=http://127.0.0.1:8000/api/v1
